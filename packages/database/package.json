{
  "name": "@ticket-platform/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node ../../scripts/noop.mjs dev @ticket-platform/database",
    "lint": "node ../../scripts/noop.mjs lint @ticket-platform/database",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/*.test.ts scripts/*.test.ts",
    "test:migrations": "node --import tsx scripts/test-migrations.ts",
    "db:migrate": "node --import tsx scripts/migrate.ts local",
    "db:migrate:deploy": "node --import tsx scripts/migrate.ts deploy",
    "db:seed": "node --import tsx scripts/seed.ts"
  },
  "dependencies": {
    "@ticket-platform/application": "workspace:*",
    "@ticket-platform/domain": "workspace:*",
    "@ticket-platform/scenario-engine": "workspace:*",
    "pg": "8.22.0"
  },
  "devDependencies": {
    "@types/pg": "8.20.0"
  }
}
