-- Пробная рассылка: тот же текст, но получатели — только чаты администраторов из
-- ADMIN_NOTIFICATION_TELEGRAM_CHAT_ID, а не аудитория по фильтрам.
--
-- Флаг живёт на самой кампании, а не в параметрах задачи: воркер разворачивает событие
-- `AdminBroadcastRequested` в получателей уже после того, как задача пролежала в очереди,
-- и должен видеть по строке кампании, боевая она или пробная. Отдельная таблица не нужна —
-- у пробного прогона та же история, те же счётчики и те же ключи идемпотентности.

alter table public.admin_broadcasts
  add column if not exists is_test boolean not null default false;

comment on column public.admin_broadcasts.is_test is
  'Пробный прогон: сообщение уходит только в административные чаты, аудитория по фильтрам игнорируется.';
