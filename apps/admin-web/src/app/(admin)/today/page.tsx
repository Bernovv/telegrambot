import { redirect } from "next/navigation";

/**
 * «Мой день» больше нет.
 *
 * Раздел был витриной чужих счётчиков: задачи он брал с доски задач, заявки — из заявок,
 * строки импорта — из базы. Ни одной собственной работы в нём не было, а заходить
 * приходилось отдельно. Числа переехали в меню и видны из любого места кабинета.
 *
 * Адрес остаётся живым: он у людей в закладках, и упереться в «страница не найдена» на
 * следующий день после пересборки — плохой способ узнать о ней.
 */
export default function TodayPage(): never {
  redirect("/tasks");
}
