import { redirect } from "next/navigation";

// Рабочий день начинается с того, что ждёт ответа и звонка, — а не со списка тех, кто
// когда-то открывал бота. Раньше корень вёл в «Мой день»; он был витриной чужих счётчиков
// и уехал в меню цифрами рядом с разделами.
export default function HomePage(): never {
  redirect("/tasks");
}
